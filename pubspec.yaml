name: tampinha
description: Aplicativo Tampinha Legal
publish_to: 'none'
version: 1.0.24+24

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  autocomplete_textfield:
  # Android, IOS, WEB
  auto_size_text: ^3.0.0
  # Android, IOS, WEB
  cached_network_image: ^3.2.3
  # Android, IOS
  cloud_firestore: ^4.1.0
  #  # Android, IOS, WEB
  cloud_functions: ^4.5.0
  # Android, IOS, WEB
  cupertino_icons: ^1.0.2
  ffi: ^2.1.0
  file_picker: ^6.0.0
  firebase_core: ^2.3.0
  firebase_messaging: ^14.1.2
  # Android, IOS
  firebase_storage: ^11.0.6
  # ANDROID IO SMACOS WEB
  firebase_auth: ^4.1.4
  # Android, IOS, WEBultimo
  flare_flutter:
  #  flutter_banner_swiper: ^0.1.4
  flutter_swiper_view: ^1.1.8
  flutter_google_places:
  flutter_launcher_icons:
  # Android, IOS
  flutter_linkify:
  # Android, IOS, WEB
  flutter_mobx:
  #  fluttertoast: ^8.2.3
  # Android, IOS, WEB
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  google_maps_flutter:
  # Android, IOS
  image_picker:
  # Android, IOS
  intl: ^0.18.0
  # Android, IOS, WEB
  location:
  liquid_progress_indicator_v2: ^0.5.0
  # ANDROID IOS LINUX MACOS WEB WINDOWS
  mobx:
  # Android, IOS, WEB
  multi_image_picker:
  # Android, IOS
  mysql1: ^0.20.0
  # Android, IOS
  permission_handler: ^11.0.0
  provider: 6.0.5
  # Android, IOS, WEB
  shimmer:
  toast:

  shared_preferences:
  # ANDROID IOS LINUX MACOS WEB WINDOWS

  url_launcher: ^6.1.12
  # Android, IOS, WEB
  webview_flutter: ^4.4.1
  # Android, IOS
  video_player:
  youtube_player_flutter: ^8.1.1

flutter_icons:
  image_path_android: "images/logoredondo.png"
  image_path_ios: "images/logoredondo.png"
  android: true
  ios: true

dev_dependencies:
  build_runner:
  mobx_codegen:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - images/